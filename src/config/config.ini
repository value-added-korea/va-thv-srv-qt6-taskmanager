; Default system configuration for La Toolhive Thv Ui
; Installed to /etc/va/la-toolhive-thv-ui/config.ini by the package.
;
; To override per-user, copy this file to:
;   ~/.config/la-toolhive-thv-ui/config.ini
; and edit the relevant values.

[general]
autostart = true

; TODO: Add your application-specific sections and keys here.
